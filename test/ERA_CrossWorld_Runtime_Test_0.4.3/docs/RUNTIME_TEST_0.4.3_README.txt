ERA CrossWorld Runtime Test 0.4.3
====================================

0.4.2 confirmed
- C# plugin build: PASS
- CALLSHARP basic call/reference return: PASS
- PluginManager.Print: PASS

0.4.2 test 3 failure
- ERH #DIM CWRT4_SHARED was passed to PluginManager.GetIntVar("CWRT4_SHARED").
- Exact Emuera source shows GetIntVar indexes VariableData.GetVarTokenDic()[name].
- Runtime KeyNotFoundException proved the user-defined #DIM name is not present there.

What this means
- User-defined ERB variables CAN be exchanged through CALLSHARP reference parameters:
  test 1 already proved int/string reference return.
- PluginManager direct name access is suitable for variables present in its VarToken dictionary,
  including built-in GLOBAL/FLAG/etc.
- PluginManager also exposes GetDataTable(name), allowing direct C# access to custom structured data.

0.4.3 tests
[1] CALLSHARP reference bridge (repeat)
[2] PluginManager.Print (repeat)
[3] Direct built-in GLOBAL:0 read/write
    ERB 12345 -> C# 13345 -> ERB confirms 13345
[4] Direct ERB-created DataTable access
    ERB creates value=500 -> C# changes to 1500 -> ERB confirms 1500
[5] C# 10,000,000-iteration benchmark
    expected checksum=342050000

Build
- Same exact pinned Emuera source commit as 0.4.2.
- Release-NAudio / x64.
- BUILD_AND_RUN_TEST.bat now ALWAYS rebuilds the plugin so a stale 0.4.2 DLL cannot be reused.
